<h3> 把我的post写到 <%= @job.title %></h3>

<hr>

<%= simple_form_for [@job, @resume] do |f| %>
  <%= f.input :content %>
  <%= f.input :attachment %>
  <%= f.input :image, as: :file %>
  <%= f.submit "送出" %>
<% end %>
