
<div class="pull-right">
   <%= link_to("Write a post", new_job_resume_path(@job), class: "btn btn-default ") %>
</div>

  <h1> <%= @job.title %></h1>

  <p>
    <%= simple_format(@job.description) %>
  </p>




  <table class="table">
    <thead>
     <tr>
        <th>文章内容</th>
        <th>图片</th>
        <th>发表者</th>
        <th>发表时间</th>
      </tr>
    </thead>
    <tbody>
      <% @resumes.each do |resume| %>
        <tr>
          <td><%= link_to(resume.content, new_job_resume_comment_path(@job.resumes, @comment))%></td>
          <td><%= link_to("See the Image", resume.attachment_url)%></td>
          <td><%= resume.user.email %></td>
          <td><%= resume.created_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
